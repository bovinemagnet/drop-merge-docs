{
  "name": "drop-merge-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:build": "antora antora-playbook.yml",
    "wiki:generate": "node scripts/generate-wiki.mjs",
    "docs:all": "npm run docs:build && npm run wiki:generate"
  },
  "devDependencies": {
    "@antora/cli": "^3.1.9",
    "@antora/site-generator-default": "^3.1.9"
  }
}
